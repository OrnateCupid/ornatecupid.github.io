---
layout: post
title: Bean 机制
tags: [bean, spring, 后端开发技巧]
---

### 1. 名称

- 对于默认使用 `@Component` 等注解的类，其 bean 名称就是类名首字母小写。**有 bean 名冲突**现象
- 对于 bean 方法返回的对象，bean 名称为方法名
- 通过 `@Controller("aaa")` 指定名称
